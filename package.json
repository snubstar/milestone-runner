{
  "name": "agent-milestone-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-orchestrator": "./dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist-test/tests/unit/*.test.js",
    "test:build": "tsc -p tsconfig.test.json && npm test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
