
1. Allow for a first routing agent to decide if the first plan needs subplans, or say, if a given milestone needs a subplan or not when it is its turn (maybe this second option better). So we avoid complex nested plans for simple tasks.

2. Allow for the user to 'load' a pre-written plan, already in the required format, so it's easier.

3. Divide the planning and implementation stage, so that there is space for the human/user, if she wants, to review the plan before implementation (the general plan)
